"""llm_hawaii — learning skeleton for Hawaiian LLM fine-tuning.

This package is intentionally minimal. It exists so a first-time
LLM-trainer can fill in the TODOs and learn the moving parts. It is
not a production pipeline and is not wired to any real corpus.

See `code/README.md` for the suggested implementation order.
"""

__all__ = ["config", "data", "model", "train", "evaluate", "metrics", "stage2_quality"]
