# Frontier model evaluation dependencies
# Semantic Kernel for provider-agnostic LLM orchestration
semantic-kernel>=1.13.0
# HTTP client for GitHub Models API
httpx>=0.27.0
# Retry logic for API calls
tenacity>=8.0.0
# OpenAI client compatibility (SK uses this internally)
openai>=1.0.0
